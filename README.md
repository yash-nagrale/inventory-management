# Inventory Management System

This is a code bundle for an Inventory Management System.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
